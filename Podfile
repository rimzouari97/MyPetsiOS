# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyPets' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
      pod 'Alamofire', '~> 5.2'
      pod 'ObjectMapper'
      pod 'PromiseKit'
      pod 'SwiftyJSON'
      pod 'Toast-Swift', '~> 5.0.0'
      pod 'SideMenu'
      

  # Pods for MyPets

  target 'MyPetsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyPetsUITests' do
    # Pods for testing
  end

end
